spring.application.name=shieldx
# ==========================================
# ? ACTIVE POSTGRESQL DATA CONFIGURATION
# ==========================================
spring.datasource.url=jdbc:postgresql://localhost:5432/ShieldX
spring.datasource.username=postgres
spring.datasource.password=Nagraj@2005
spring.datasource.driver-class-name=org.postgresql.Driver

# ==========================================
# ?? HIBERNATE / JPA ORM SETTINGS
# ==========================================
# Automatically creates or alters SQL tables to match your new User and ParentChildConnection models
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ==========================================
# ?? ADDITIONAL SERVER CONFIGURATIONS
# ==========================================
spring.main.allow-bean-definition-overriding=true

# (Ensure all spring.data.mongodb lines are completely deleted or commented out!)

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=shieldx.app@gmail.com
spring.mail.password=ykjt qwsr inju lker
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
